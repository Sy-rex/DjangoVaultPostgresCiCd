name: Deploy Django App

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

env:
  K8S_NAMESPACE: django-app

jobs:
  build-and-deploy:
    # ВАЖНО: используем ваш self-hosted runner
    runs-on: self-hosted 
    
    permissions:
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Kubernetes context
        run: |
          kubectl config use-context minikube

      - name: Build Django Docker image in Minikube
        run: |
          # Настраиваем Docker для использования minikube docker daemon
          eval $(minikube docker-env)
          
          # Собираем образ Django из Dockerfile в minikube docker daemon
          docker build -t django-kubernetes-app:latest .
          
          # Сбрасываем docker env
          eval $(minikube docker-env -u) 

      - name: Build Vault Docker image in Minikube
        run: |
          # Настраиваем Docker для использования minikube docker daemon
          eval $(minikube docker-env)
          
          # Собираем образ Vault из Dockerfile в minikube docker daemon
          docker build -t vault-custom:latest -f vault/Dockerfile vault/
          
          # Сбрасываем docker env
          eval $(minikube docker-env -u)

      - name: Deploy and configure Vault
        run: |
          # Создаем namespace vault
          kubectl create namespace vault --dry-run=client -o yaml | kubectl apply -f -
          
          # Применяем манифесты Vault
          kubectl apply -f k8s-manifests/vault/namespace.yaml
          kubectl apply -f k8s-manifests/vault/deployment.yaml
          kubectl apply -f k8s-manifests/vault/service.yaml
          
          # Ожидаем готовности Vault pod
          echo "⏳ Ожидаем готовности Vault..."
          kubectl wait --for=condition=ready pod -l app=vault -n vault --timeout=120s || true
          
          # Даем время на полную инициализацию
          sleep 15
          
          # Получаем имя pod Vault
          VAULT_POD=$(kubectl get pods -n vault -l app=vault -o jsonpath='{.items[0].metadata.name}')
          
          # Устанавливаем переменные окружения для Vault (dev режим использует root токен)
          export VAULT_ADDR="http://localhost:8200"
          export VAULT_TOKEN="root"
          
          # Настраиваем KV движок версии 2 (если еще не включен)
          echo "📝 Настраиваем KV движок версии 2..."
          kubectl exec $VAULT_POD -n vault -- sh -c 'VAULT_ADDR=http://localhost:8200 VAULT_TOKEN=root vault secrets enable -path=secret kv-v2' || echo "KV движок уже включен"
          
          # Записываем секреты для Django приложения
          echo "💾 Записываем секреты для Django приложения..."
          kubectl exec $VAULT_POD -n vault -- sh -c 'VAULT_ADDR=http://localhost:8200 VAULT_TOKEN=root vault kv put secret/django-app/database \
            db_name="django_prod" \
            db_user="django_user" \
            db_password="SuperSecret123!" \
            secret_key="very-secret-key-prod"'
          
          # Создаем политику доступа
          echo "📋 Настраиваем политику доступа..."
          kubectl exec $VAULT_POD -n vault -- sh -c 'echo "path \"secret/data/django-app/*\" { capabilities = [\"read\"] }" | VAULT_ADDR=http://localhost:8200 VAULT_TOKEN=root vault policy write django-app -' || echo "Политика уже существует"
          
          # Проверяем секреты
          echo "🔍 Проверяем записанные секреты..."
          kubectl exec $VAULT_POD -n vault -- sh -c 'VAULT_ADDR=http://localhost:8200 VAULT_TOKEN=root vault kv get secret/django-app/database' || true
          
          echo "✅ Vault успешно установлен и настроен!"

      - name: Deploy to Minikube
        run: |
          # Создаем namespace если нет
          kubectl create namespace ${{ env.K8S_NAMESPACE }} --dry-run=client -o yaml | kubectl apply -f -
          
          # Секреты (См. пояснение ниже - т.к. это CI, секрет должен уже быть или создаваться тут)
          # В лабе предполагается ручное создание, поэтому пропустим создание секрета в CI
          
          # Применяем манифесты
          kubectl apply -f k8s-manifests/namespace.yaml
          kubectl apply -f k8s-manifests/configmap.yaml
          kubectl apply -f k8s-manifests/postgres/
          kubectl apply -f k8s-manifests/deployment.yaml
          kubectl apply -f k8s-manifests/service.yaml
          kubectl apply -f k8s-manifests/ingress.yaml
          
          # Форсируем обновление образа (rollout restart)
          kubectl rollout restart deployment/django-deployment -n ${{ env.K8S_NAMESPACE }}
